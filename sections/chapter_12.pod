=encoding utf8

=head0 还缺少什么

单就 Perl 自身来看似乎并不完美，但它是可扩展的---这样设计的部分原因在于不存
在某一个特定的设计方案能够做到同时满足每一个编程者和每一个应用的需求。一些
有用的部分存在于语言核心库中。更多的可用内容则在 CPAN ( http://cpan.org )。
高效的 Perl 开发者能够充分利用手中的各类资源。

L<missing_defaults>
